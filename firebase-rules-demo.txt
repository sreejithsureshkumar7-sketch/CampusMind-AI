rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /students/{studentId} {
      allow read, write: if true;
    }
  }
}

IMPORTANT:
This rule is only for college demo/testing. Do not store real sensitive student details with open rules.
For real project, add Firebase Authentication and restrict write access to admin users.
